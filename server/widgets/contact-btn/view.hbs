{{#unless widget.disabled}}
  <we-contact-btn data-userid="{{locals.req.user.id}}" data-contactuserid="{{locals.user.id}}" class="btn btn-default">{{t 'contact-btn-text-add' }}</we-contact-btn>
{{/unless}}