{
  "name": "we-plugin-contact",
  "description": "We.js contact plugin",
  "version": "0.3.1",
  "license": "MIT",
  "main": "lib/index.js",
  "dependencies": {
    "async": "^0.9.0",
    "lodash": "^3.0.1"
  },
  "devDependencies": {
    "fs-extra": "^0.18.2",
    "istanbul": "^0.2.7",
    "jscs": "^1.4.5",
    "jshint": "^2.5.0",
    "mocha": "^1.18.2",
    "rimraf": "^2.3.3",
    "supertest": "^0.15.0",
    "we-test-tools": "^0.3.0",
    "we-core": "^0.3.19",
    "we-theme-admin-default": "^0.3.1",
    "we-theme-site-wejs": "^0.3.6"
  },
  "engines": {
    "node": ">=0.10.0"
  },
  "scripts": {
    "lint": "./node_modules/.bin/jshint .",
    "test": "NODE_ENV=test LOG_LV=info ./node_modules/.bin/mocha test/bootstrap.js test/**/*.test.js",
    "prepublish": "npm prune && npm run lint"
  },
  "keywords": [
    "we.js",
    "wejs",
    "plugin",
    "contact"
  ],
  "repository": {
    "type": "git",
    "url": "git@github.com:wejs/we-plugin-contact.git"
  }
}
